import os
import database
import add_test_zones


def init_on_render():
    """Инициализация базы данных при запуске на Render"""
    print("🚀 Инициализация базы данных на Render...")

    # Удаляем старую базу если есть
    if os.path.exists('/tmp/green_city.db'):
        os.remove('/tmp/green_city.db')

    # Инициализируем новую базу
    database.init_db()

    # Добавляем тестовые зоны
    print("📝 Добавление тестовых зон...")
    try:
        add_test_zones.add_test_zones()
        print("✅ Тестовые зоны добавлены!")
    except Exception as e:
        print(f"⚠️ Ошибка при добавлении тестовых зон: {e}")

    print("🎉 База данных готова!")


if __name__ == '__main__':
    init_on_render()