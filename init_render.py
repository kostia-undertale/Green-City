import os
import database
import add_test_zones

print("🚀 Начинаем инициализацию базы данных на Render...")

# Удаляем старую базу если существует
db_path = '/tmp/green_city.db'
if os.path.exists(db_path):
    os.remove(db_path)
    print("🗑️ Удалена старая база данных")

# Инициализируем новую базу
print("🔄 Создание новой базы данных...")
database.init_db()

# Добавляем тестовые зоны
print("📝 Добавление тестовых зон...")
try:
    add_test_zones.add_test_zones()
    print("✅ Тестовые зоны успешно добавлены!")
except Exception as e:
    print(f"⚠️ Ошибка при добавлении тестовых зон: {e}")

print("🎉 Инициализация завершена!")
print(f"📊 База данных создана по пути: {db_path}")